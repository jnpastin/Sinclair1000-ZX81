'
'Load the machine code into memory and execute it.  This is easier than poking it.
'
10 REM YHNOT YENOT YLNOT NOT YONOT YRNOT YLNOT YDNOT Y.NOT TAN 		'Load the machine code (via Sinclair character codes) into memory
20 RAND USR 16514							'Execute the machine code
